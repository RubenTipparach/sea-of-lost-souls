{
  "name": "sol-ship-editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Web-based ship editor for Sea of Lost Souls (authors model.glb + ship.json).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  }
}
